# 片刻 2.0.0

发布日期：2026-08-31
状态：候选发布

“片刻”现在是一处会随时间呼吸的林间冥想空间，同时保持本地优先、无账号与可靠计时。

## 本版内容

- 晨、昼、暮、夜四种随设备本地时间变化的抽象林间光影；
- 每日稳定的时段短句，以及沉静为抽象呼吸光场的冥想过渡；
- 首次默认 5 分钟，首页快捷时长精简为 5、10、20 分钟；
- 1～60 分钟精细时长、环境声和结束提示音统一收进设置面板；
- 空记录不再显示“0 秒”，有记录后改为温和的今日累计提示；
- 程序化生成的风、树叶、水纹和远鸟声，无外部音频资源；
- 旧 `musicEnabled` 偏好自动迁移为环境声开关，旧主题值安全忽略；
- 原有暂停、恢复、提前结束、自然完成、统计、PWA 和离线能力保持不变；
- 支持键盘、读屏、200% 字号与减少动态效果。

## 隐私与降级

- 没有账号、广告、分析脚本、地理位置请求或业务服务器；
- 偏好和记录保存在浏览器 IndexedDB 中；
- Web Audio、持久化或动效能力不可用时，核心计时流程仍可独立运行；
- 清除站点数据、使用隐私浏览或更换浏览器／设备会使本地记录不可用。
# 2.1.0 · 自主体验优化（2026-09-05）

- 重组首页层级，时长、快捷选择、环境声和开始操作集中呈现。
- 首页直接查看最近 15 天记录；空记录给出说明，与完成页共用记录组件。
- 修复计时中的声音文案与勾选状态不同步；键盘焦点所在的操作区不会自动隐藏。
- 设置及结束确认启用背景交互隔离；计时页增加可用键盘访问的操作入口。
- 时长保存和开始失败提供反馈，失败后支持再次尝试。
- 更新离线缓存版本；本地启动使用与构建一致的配置加载方式。
- 数据库结构未变更，可切回优化前版本，详见 `docs/09-self-optimization.md`。
# 2.1.1 · 离线启动修复（2026-09-12）

- 离线安装必须成功缓存全部 HTML、CSS、JavaScript 与图标；资源缺失或返回错误类型时拒绝激活不完整版本。
- 导航始终使用当前完整缓存版本的 HTML，防止 HTML 更新后对应脚本尚未缓存导致白屏。
- 开发模式通过 Vite 标志识别，清理范围缩小为当前应用的 Worker 注册，保留已有缓存。
- 缓存版本升为 v10；新增断网重开、缺失 CSS、JS 返回 HTML、子路径与浏览器拒绝存储的行为测试。
