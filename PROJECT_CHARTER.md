# 极简冥想计时器：项目总纲

> 版本：1.0  
> 状态：产品方向与关键决策已确认  
> 更新日期：2026-08-04  
> 用途：本文件是后续产品、交互、视觉、前端、测试与发布工作的最高级项目约束。新任务开始时必须先阅读本文件，再开展对应阶段工作。

## 1. 项目使命

制作一个极简、高级、安静的纯网页冥想计时器，让用户从打开页面到开始冥想只经历必要步骤。

核心路径：

**打开 → 选择时间 → 开始 → 安静等待 → 完成 → 可选查看记录**

衡量任何方案时，按以下顺序判断：

1. 是否让用户更快开始冥想；
2. 是否减少冥想过程中的注意力干扰；
3. 是否清晰、稳定、容易理解；
4. 是否维持无账号、无服务器、本地优先；
5. 删除该功能是否会损害核心体验。

项目优先级：

**安静感 > 操作清晰 > 计时可靠 > 可访问性 > 视觉表现 > 功能丰富**

## 2. 产品形态与边界

### 2.1 产品形态

- 纯网页应用，支持电脑和手机浏览器；
- 支持响应式布局；
- PWA 安装为增强能力，普通网页必须独立完整可用；
- 不制作原生手机 App；
- 不制作桌面客户端；
- 不依赖业务服务器；
- 不需要账号；
- 用户数据只保存在本地。

### 2.2 当前版本明确不做

- 账号、登录与云同步；
- 社交、分享、排行榜；
- 积分、等级、徽章和强迫连续打卡；
- 冥想课程、社区内容和订阅支付；
- 推送通知；
- 复杂设置中心；
- 超出最近 15 天的长期分析。

不得以“为未来预留”为理由提前引入服务器、身份系统或复杂数据架构。

## 3. 已确认且冻结的产品决策

除非出现明确的可用性、无障碍或技术阻断，后续阶段不得反复讨论以下选择：

| 项目 | 已确认方案 |
|---|---|
| 默认主题 | 方案 B：自然矿石感 |
| 可切换主题 | 方案 C：柔和晨雾感 |
| 圆环动画 | 以剩余时间进度为主，叠加极轻微呼吸 |
| 首页统计入口 | 首页底部显示轻量摘要 |
| 完成页趋势 | 小型柱状图 |
| 完成页布局 | 渐进展开 |
| 默认冥想时长 | 20 分钟 |
| 自定义时间范围 | 1～60 分钟 |
| 完成提示音 | 默认开启 |
| 数据保存范围 | 最近 15 个自然日 |

主题切换属于已确认需求：默认使用自然矿石感，用户可切换到柔和晨雾感，选择在本地记忆。主题入口应保持次级，不在冥想进行页出现。

## 4. 功能范围

### 4.1 计时

必须支持：

- 快捷时间：5、10、15、20、30 分钟；
- 通过分钟滑块选择 1～60 分钟；
- 开始；
- 暂停；
- 继续；
- 提前结束；
- 自然完成；
- 提前结束时记录实际冥想时长。

计时必须以真实时间差为依据。动画帧、定时回调次数或页面是否处于前台，均不得成为计时真值来源。

### 4.2 本地记录

每天只需保存：

- 日期；
- 总冥想时长；
- 完成次数。

展示：

- 今日累计时长；
- 最近 7 天累计；
- 最近 15 天趋势；
- 完成页展开后的轻量完成次数信息。

规则：

- 同一天多次冥想自动累计；
- 提前结束计入一次完成并记录实际时长；
- 以秒作为内部时长单位，界面按需要转为分钟和秒；
- 只保留展示所需的最近 15 天日汇总；
- 同一次冥想不得重复结算；
- 本地数据缺失或异常时，应用必须安全回到空记录状态。

### 4.3 声音

- 完成提示音默认开启；
- 用户可关闭，并在本地记忆选择；
- 自然完成时只播放一次；
- 提前结束不播放完整完成音；
- 声音不可用时不得阻断完成流程，也不得出现侵入式错误提示。

## 5. 信息架构：只保留三个主要页面

### 5.1 首页

唯一首要任务：选择时长并开始。

信息层级：

1. 冥想标题；
2. 当前选择时长；
3. 快捷时间；
4. 分钟滑块；
5. 开始按钮；
6. 页面底部的今日与近 7 天轻量统计；
7. 主题、声音等次级控制。

要求：

- 默认选中 20 分钟；
- 点击快捷值时滑块同步；
- 拖动滑块时当前分钟数即时更新；
- 统计不得进入视觉中心；
- 不用欢迎弹窗、教程或安装提示阻挡开始路径。

### 5.2 冥想进行页

唯一首要任务：提供安静、可信的计时环境。

默认只显示：

- 呼吸进度圆环；
- 剩余时间。

点击屏幕空白区域后临时显示：

- 暂停或继续；
- 提前结束。

要求：

- 默认隐藏操作控制；
- 控制出现时圆环和时间不得发生位置跳动；
- 无操作后控制自然淡出；
- 暂停状态下“继续”保持可见，状态必须明确；
- 提前结束需要一次简短确认以防误触；
- 不显示统计、主题、菜单、说明或多余文字。

### 5.3 完成页

首要任务：确认结果，提供轻微而克制的反馈。

初始显示：

- 一句中性结束语；
- 本次完成时间；
- 今日累计时间；
- 返回首页；
- 再次冥想；
- “查看最近记录”入口。

展开后显示：

- 最近 7 天累计与近期数据；
- 最近 15 天小型柱状趋势；
- 完成次数等次级信息。

不得使用庆祝爆炸、彩带、积分、等级、失败提示或制造打卡压力的文案。

## 6. 核心状态模型

```text
准备
  └─ 开始 → 进行中
               ├─ 暂停 → 已暂停 ─ 继续 → 进行中
               ├─ 提前结束并确认 → 已完成
               └─ 时间归零 → 已完成

已完成
  ├─ 返回首页 → 准备
  └─ 再次冥想 → 准备／进行中（由交互规格确定）
```

交互和开发必须补齐以下异常状态：

- 页面刷新；
- 浏览器切入后台；
- 设备锁屏后重新唤醒；
- 页面被关闭后重新打开；
- 系统时间发生变化；
- 本地记录损坏；
- 提示音播放失败；
- 用户开启减少动态效果；
- PWA 不可安装或安装失败。

状态恢复不得造成倒计时漂移、重复记录或完成音重复播放。

## 7. 视觉总纲

### 7.1 共同视觉关键词

**安静、克制、留白、稳定、柔和、低刺激、有触感。**

高级感主要依靠：

- 准确的信息层级；
- 稳定的构图；
- 充足留白；
- 克制的色彩数量；
- 字体、间距和细线细节；
- 平稳且有目的的动效。

不得依靠廉价渐变、大面积发光、玻璃拟态堆叠、粒子、复杂纹理或大量装饰制造高级感。

### 7.2 主题 B：自然矿石感（默认）

候选基准色：

- 背景：温暖石灰 `#D8D3C8`；
- 表层：浅岩灰 `#C9C3B7`；
- 主文字：炭褐 `#33322E`；
- 次级文字：岩灰 `#77736A`；
- 圆环强调：矿石绿 `#657A70`。

视觉阶段允许为可读性和对比度微调色值，但不得改变其温暖、自然、稳定的主题身份。

### 7.3 主题 C：柔和晨雾感（可切换）

候选基准色：

- 背景：雾白蓝 `#E4E8E6`；
- 表层：柔白 `#F0F2EF`；
- 主文字：深雾灰 `#39413F`；
- 次级文字：蓝灰 `#7D8885`；
- 圆环强调：雾蓝 `#839E9C`。

视觉阶段允许为可读性和对比度微调色值，但不得改变其轻盈、清洁、通透的主题身份。

### 7.4 主题一致性

主题切换只能改变：

- 背景与表层颜色；
- 主次文字颜色；
- 圆环、边框和按钮的色彩关系；
- 必要的明暗参数。

主题切换不得改变布局、字号、信息顺序、交互方式或动画节奏。

## 8. 动效与无障碍原则

### 8.1 呼吸进度圆环

- 进度变化承担剩余时间表达；
- 呼吸只营造轻微生命感，不承担“吸气／呼气”训练指令；
- 呼吸幅度小、周期缓慢；
- 进度变化连续稳定；
- 暂停时进度与呼吸同步停止；
- 不使用闪烁、扫光、粒子或明显弹性动画。

### 8.2 页面转场与控制

- 使用低幅度淡入淡出；
- 不使用强烈缩放和整页横向滑动；
- 动画不得延迟用户操作；
- 控制按钮淡入后不改变主体构图；
- 用户开启减少动态效果时，取消呼吸缩放和非必要转场，保留清晰的静态进度表达。

### 8.3 基本可访问性

- 文字与背景必须保持足够对比；
- 触控目标适合手机单手操作；
- 关键操作支持键盘访问；
- 状态不能只依靠颜色表达；
- 屏幕阅读器能够理解时间、计时状态和主要按钮；
- 字号放大后不得遮挡核心操作。

## 9. 技术架构原则

正式技术选型在技术架构阶段确定，但必须满足：

- 单一前端应用；
- 无业务后端接口；
- 无账号和远程数据库；
- 核心计时不依赖网络；
- 计时状态、视觉动画和统计计算相互分离；
- 页面共用唯一计时状态来源；
- 页面共用唯一记录与统计逻辑；
- 主题通过统一设计变量管理；
- PWA 层不得侵入核心计时流程；
- 优先选择体积小、成熟、可维护的方案，避免无必要的大型依赖。

建议模块边界：

1. 计时状态；
2. 计时恢复与结算；
3. 本地记录；
4. 统计计算；
5. 主题；
6. 声音；
7. 页面状态与转场；
8. PWA 与离线；
9. 无障碍适配。

## 10. 分阶段路线与交付门槛

| 阶段 | 主要任务 | 必须交付 | 进入下一阶段的条件 |
|---|---|---|---|
| 0. 总纲冻结 | 固定目标、范围和关键选择 | 本文件 | 已完成 |
| 1. 低保真与交互 | 设计三页的电脑、手机布局及全部状态 | 页面线框、流程、交互规格 | 无说明情况下能理解开始、暂停、继续、结束和查看记录 |
| 2. 视觉系统 | 落实两套主题和组件规范 | 高保真稿、设计变量、组件与排版规范 | 两主题一致、可读、安静，关键页面完成响应式设计 |
| 3. 动效原型 | 验证圆环、控制显隐和页面转场 | 可交互原型、动效参数 | 动效不干扰计时理解，减少动态模式成立 |
| 4. 技术架构 | 固化状态、数据、模块和测试策略 | 技术架构文档、数据约定 | 刷新、后台、重复结算等关键风险有明确方案 |
| 5. 核心开发 | 完成三页、计时、记录和统计 | 可运行 MVP | 自然完成和提前结束均准确，核心流程完整 |
| 6. 体验增强 | 完成主题、声音、恢复、离线和 PWA | 候选发布版本 | 增强能力失败时不影响普通网页核心体验 |
| 7. 测试验收 | 跨设备、浏览器、性能、无障碍检查 | 测试报告、修复清单 | 无阻断流程问题，关键验收项通过 |
| 8. 发布 | 静态部署与发布复核 | 正式版本、发布说明 | 可稳定访问、计时、记录并按支持条件安装 |
| 9. 克制迭代 | 根据真实问题优化 | 排序后的迭代清单 | 所有新增项通过本总纲审查 |

## 11. 下一任务的唯一目标

下一任务进入 **阶段 1：低保真与完整交互规格**。

必须交付一份可以同时指导视觉设计和前端实现的文档，建议文件名：

`docs/01-ux-spec.md`

该阶段需要完成：

1. 首页、冥想进行页、完成页的电脑线框；
2. 三个页面的手机线框；
3. 准备、进行、控制显现、暂停、结束确认、自然完成、提前结束、趋势展开等状态；
4. 所有点击、滑动、自动隐藏、返回和恢复行为；
5. 页面信息层级、主要尺寸关系和响应式变化；
6. 空记录、单日记录和 15 天记录等数据展示状态；
7. 可能误触或产生歧义的边界情况；
8. 阶段验收清单。

阶段 1 不应：

- 创建正式前端项目；
- 编写业务代码；
- 重新选择已冻结的产品方案；
- 过早确定框架或依赖；
- 扩展第四个主要页面；
- 增加本总纲范围外的功能。

## 12. 总体验收标准

### 12.1 功能

- 默认时间为 20 分钟；
- 快捷时间和 1～60 分钟滑块正确联动；
- 开始、暂停、继续、提前结束和自然完成状态正确；
- 页面进入后台后剩余时间仍准确；
- 提前结束记录实际时长；
- 同一天多次冥想正确累计；
- 最近 7 天累计和最近 15 天趋势正确；
- 只保留最近 15 天记录；
- 同一次冥想不会被重复结算。

### 12.2 体验

- 用户进入首页后无需学习即可开始；
- 统计不会抢占首页视觉中心；
- 进行页默认没有多余控制和信息；
- 控制显隐不造成主体跳动；
- 完成反馈克制且不游戏化；
- 完成页趋势由用户主动展开；
- 两套主题具有一致布局和清晰层级。

### 12.3 稳定性

- 刷新、后台、锁屏和恢复不会造成明显计时漂移；
- 本地数据异常不会让页面无法打开；
- 声音失败不影响完成；
- PWA 不可用时普通网页完整可用；
- 低性能设备上的圆环动画平稳且不过度耗电。

## 13. 变更控制

普通调整可以在对应阶段自行完成，但以下变化必须由用户重新确认：

- 增加主要页面；
- 引入账号、服务器或云同步；
- 改变记录范围；
- 改变默认主题或默认时长；
- 改变提前结束的记录规则；
- 增加游戏化机制；
- 在进行页增加长期可见的信息或控制；
- 改变已确认的趋势展示与完成页展开方式。

发生分歧时，优先回到项目使命与最高设计原则，不以功能数量、装饰效果或技术新颖度作为决定依据。

## 14. 新任务启动指令

可将以下文字作为下一任务的开场指令：

> 请先完整阅读工作区根目录的 `PROJECT_CHARTER.md`，以其中已冻结的产品决策和阶段边界为最高约束。现在只执行阶段 1：为极简冥想计时器产出三页面的电脑与手机低保真结构、全部关键状态和完整交互规格，并生成 `docs/01-ux-spec.md`。不要创建前端项目，不要编写代码，不要重新讨论已经确认的颜色、圆环、统计入口、趋势形式、默认时长、时间范围和提示音选择。

