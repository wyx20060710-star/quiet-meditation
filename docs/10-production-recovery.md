# 2.1.1 正式版本与离线修复

日期：2026-09-12。

正式入口：https://quiet-meditation.pages.dev/

## 发布前验证

75 项自动化测试、TypeScript 检查和生产构建通过。新增行为测试验证：完整缓存后模拟断网可读取 HTML、JS、CSS；必需资源失败不激活新 Worker；将 HTML 错当成 JS 的响应被拒绝；联网时不把不匹配的新 HTML 写入旧缓存。

生产基线提交：`ea2eaf2bceb1edd0fdeb54c525faad8c9ed01bb0`。其文件树与本地原版 `7c6d343` 完全一致（`fdbcecff63727b8903f983eb01dd1f80b0bc767d`），因此本次以该生产提交为父提交发布已验证的变更。

## 正确安装和数据

从正式 HTTPS 地址通过 Edge 的“将此站点安装为应用”安装。首次安装需联网，等首页完整显示后再关闭。正式站点的离线资源不依赖本机 Node 或 Vite 服务。

旧本地入口（例如 `127.0.0.1:5173`）与正式域名属于不同存储来源，记录不会自动迁移。不要卸载旧入口并清除数据；旧记录需要在原来源恢复后读取。切换入口不会删除原来源的数据。

## 回滚

代码基线标签 `rollback/pre-self-optimize-20260905` 保留。线上回滚可在 Cloudflare Pages 中选择本次发布前成功的生产部署并执行回滚；或在 GitHub 恢复生产基线文件树、生成新的提交并重新发布。避免只替换 HTML。此次无数据库迁移。

发布状态：待云端部署验证。
